# #workflow
# title: Website name
# description: A website for uplaoding content
# age: 30
# tags:
#  - technology
#  - images
#  - creative
# address:
#   street: mg road
#   city: pune
#   state: na
# value: ${{env.VALUE}}
# variable: ${{github.VARIABLE_NAME}}

name: github action ofr
on:
  push:
    branches: [main]
jobs:
  build:
     runs-on: ubuntu-latest
     steps:
       - uses: actions/checkout@v3
       - name: Print node version
         run: node --version
       - name: Print NPM version
         run: npm --version
       - name: Print pwd
         run: pwd


